# 🏗️ Architecture Diagrams

## 1. Overall System Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER REQUEST                             │
│         GET /api/flights/search?origin=BOM&destination=DXB       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FLIGHT CONTROLLER                             │
│  • Parse query parameters                                        │
│  • Build searchParams & options                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FLIGHT SERVICE (WRAPPER) 🔥                   │
│  1. Validate search params                                       │
│  2. Check Redis cache                                            │
│  3. Get active suppliers from config                             │
│  4. Call suppliers in PARALLEL ────────┐                        │
│  5. Aggregate results                   │                        │
│  6. Build industry response             │                        │
│  7. Cache & return                      │                        │
└─────────────────────────────────────────┼───────────────────────┘
                                          │
        ┌─────────────────────────────────┼─────────────────────┐
        │                                 │                     │
        ▼                                 ▼                     ▼
  ┌─────────────┐                   ┌─────────────┐      ┌─────────────┐
  │   AMADEUS   │                   │    SABRE    │      │ TRAVELPORT  │
  │   API Call  │                   │  API Call   │      │  API Call   │
  │  (7s timeout)                   │ (7s timeout)│      │ (7s timeout)│
  └──────┬──────┘                   └──────┬──────┘      └──────┬──────┘
         │                                 │                     │
         ▼                                 ▼                     ▼
  ┌─────────────┐                   ┌─────────────┐      ┌─────────────┐
  │  ASSEMBLY   │                   │  ASSEMBLY   │      │  ASSEMBLY   │
  │    LINE     │                   │    LINE     │      │    LINE     │
  │ Map→Validate│                   │ Map→Validate│      │ Map→Validate│
  │ →Transform  │                   │ →Transform  │      │ →Transform  │
  └──────┬──────┘                   └──────┬──────┘      └──────┬──────┘
         │                                 │                     │
         └─────────────────────────────────┴─────────────────────┘
                                   │
                                   ▼
                          ┌─────────────────┐
                          │   AGGREGATOR    │
                          │  • Merge all    │
                          │  • Deduplicate  │
                          │  • Filter       │
                          │  • Sort         │
                          │  • Paginate     │
                          └────────┬────────┘
                                   │
                                   ▼
                    ┌──────────────────────────┐
                    │  DICTIONARIES BUILDER    │
                    │  • Extract airline codes │
                    │  • Extract airport codes │
                    │  • Build lookup tables   │
                    └────────┬─────────────────┘
                             │
                             ▼
                    ┌──────────────────────────┐
                    │  RESPONSE BUILDER        │
                    │  • meta                  │
                    │  • search                │
                    │  • data[]                │
                    │  • dictionaries          │
                    └────────┬─────────────────┘
                             │
                             ▼
                    ┌──────────────────────────┐
                    │   REDIS CACHE (5 min)    │
                    └────────┬─────────────────┘
                             │
                             ▼
                    ┌──────────────────────────┐
                    │    JSON RESPONSE         │
                    │  (Industry Standard)     │
                    └──────────────────────────┘
```

---

## 2. Assembly Line Pattern

```
┌─────────────────────────────────────────────────────────────────┐
│                    RAW SUPPLIER DATA                             │
│  Different formats from Amadeus, Sabre, Travelport, etc.        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │    MAPPER    │
                  │              │
                  │ Converts:    │
                  │ Amadeus →    │
                  │ Standard     │
                  │              │
                  │ Sabre →      │
                  │ Standard     │
                  └──────┬───────┘
                         │
                         ▼
            ┌─────────────────────────┐
            │  STANDARD OFFER FORMAT  │
            │  (NDC/OTA-compliant)    │
            └───────────┬─────────────┘
                        │
                        ▼
                 ┌──────────────┐
                 │  VALIDATOR   │
                 │              │
                 │ Checks:      │
                 │ • Structure  │
                 │ • Pricing    │
                 │ • Dates      │
                 │ • Logic      │
                 └──────┬───────┘
                        │
                        ▼
            ┌─────────────────────────┐
            │    VALID OFFERS ONLY    │
            └───────────┬─────────────┘
                        │
                        ▼
                 ┌──────────────┐
                 │ TRANSFORMER  │
                 │              │
                 │ Adds:        │
                 │ • Formatted  │
                 │   times      │
                 │ • Layovers   │
                 │ • Computed   │
                 │   fields     │
                 └──────┬───────┘
                        │
                        ▼
            ┌─────────────────────────┐
            │   ENRICHED OFFERS       │
            │  Ready for aggregation  │
            └─────────────────────────┘
```

---

## 3. Deduplication Logic

```
Supplier Results:

Amadeus:
┌────────────────────────────────────┐
│ EK501 BOM→DXB 10:30 Economy ₹21,500│
│ AI101 BOM→DXB 14:00 Economy ₹19,000│
└────────────────────────────────────┘

Sabre:
┌────────────────────────────────────┐
│ EK501 BOM→DXB 10:30 Economy ₹22,000│  ← DUPLICATE (higher price)
│ 6E204 BOM→DXB 08:00 Economy ₹18,500│
└────────────────────────────────────┘

Travelport:
┌────────────────────────────────────┐
│ AI101 BOM→DXB 14:00 Economy ₹18,800│  ← DUPLICATE (lower price) ✅
│ UK890 BOM→DXB 16:00 Economy ₹23,000│
└────────────────────────────────────┘

          ↓ DEDUPLICATION KEY
          airline + flight + departure + cabin
          ↓

Final Result (keeping cheapest):
┌────────────────────────────────────┐
│ 6E204 BOM→DXB 08:00 Economy ₹18,500│  From Sabre
│ AI101 BOM→DXB 14:00 Economy ₹18,800│  From Travelport ✅
│ EK501 BOM→DXB 10:30 Economy ₹21,500│  From Amadeus ✅
│ UK890 BOM→DXB 16:00 Economy ₹23,000│  From Travelport
└────────────────────────────────────┘
```

---

## 4. Caching Strategy

```
Request → Service → Cache Check
                         │
              ┌──────────┴──────────┐
              │                     │
              ▼                     ▼
         CACHE HIT             CACHE MISS
         (~10ms)               (~2000ms)
              │                     │
              │                     ▼
              │              Call Suppliers
              │                     │
              │                     ▼
              │              Process Data
              │                     │
              │                     ▼
              │              Cache Result
              │                     │
              └──────────┬──────────┘
                         │
                         ▼
                    Return Response

Cache Key Format:
flights:bom:dxb:2025-12-25:ow:1:0:0:economy:price:1
│       │   │   │           │  │ │ │ │       │     │
│       │   │   │           │  │ │ │ │       │     └─ Page
│       │   │   │           │  │ │ │ │       └─────── Sort
│       │   │   │           │  │ │ │ └─────────────── Cabin
│       │   │   │           │  │ │ └───────────────── Infants
│       │   │   │           │  │ └─────────────────── Children
│       │   │   │           │  └───────────────────── Adults
│       │   │   │           └──────────────────────── One Way
│       │   │   └──────────────────────────────────── Date
│       │   └──────────────────────────────────────── Destination
│       └──────────────────────────────────────────── Origin
└──────────────────────────────────────────────────── Prefix
```

---

## 5. Response Format Structure

```json
{
  "meta": {                          ← Technical metadata
    "currency": "INR",
    "totalResults": 120,
    "page": 1,
    "pageSize": 50,
    "sorting": "PRICE_ASC",
    "suppliersUsed": ["amadeus"],
    "requestId": "...",
    "responseTimeMs": 1234
  },
  "search": {                        ← Echo of search params
    "origin": "BOM",
    "destination": "DXB",
    "departureDate": "2025-12-25",
    "tripType": "ONE_WAY",
    "adults": 1
  },
  "data": [                          ← Flight offers
    {
      "id": "OFFER_1",
      "source": {                    ← Supplier info
        "supplier": "amadeus",
        "supplierOfferId": "XYZ123",
        "distributionChannel": "GDS"
      },
      "itinerary": {                 ← Flight details
        "slices": [                  ← Outbound/Inbound
          {
            "sliceId": "OUTBOUND",
            "segments": [             ← Flight legs
              {
                "marketingAirlineCode": "EK",
                "flightNumber": "501",
                "departure": {...},
                "arrival": {...}
              }
            ]
          }
        ]
      },
      "pricing": {...}               ← Price breakdown
    }
  ],
  "dictionaries": {                  ← Lookup tables
    "airlines": {
      "EK": {"name": "Emirates"}
    },
    "airports": {
      "BOM": {"name": "...", "countryCode": "IN"}
    },
    "aircraft": {
      "77W": {"name": "Boeing 777-300ER"}
    }
  }
}
```

---

## 6. Adding New Supplier (Plug & Play)

```
Step 1: Create Adapter
src/suppliers/sabre/
├── sabre.client.js      → HTTP client (token, auth)
├── sabre.flight.js      → API calls
└── index.js             → Export

Step 2: Add Mapper
src/assembly_line/mappers/flight.mapper.js
+ mapSabreData() function
  → Convert Sabre format → Standard format

Step 3: Add to Config
src/config/suppliers.config.js
+ sabre: {
    name: 'Sabre',
    code: 'sabre',
    isActive: true,        ← TURN ON
    timeout: 7000,
    priority: 2
  }

Step 4: Update Factory
src/suppliers/supplierFactory.js
+ sabre: Sabre

          ↓
   ✅ AUTOMATIC!
   Service now calls Sabre in parallel
   No changes to service/controller needed!
```

---

## 7. Aggregation Pipeline

```
┌────────────────────────────────────────────────────┐
│           SUPPLIER RESULTS (3 arrays)               │
│  [Amadeus: 50 offers]                              │
│  [Sabre: 45 offers]                                │
│  [Travelport: 40 offers]                           │
└───────────────────┬────────────────────────────────┘
                    │
                    ▼
            ┌───────────────┐
            │  MERGE (135)  │
            └───────┬───────┘
                    │
                    ▼
            ┌───────────────┐
            │ DEDUPLICATE   │
            │  (135 → 120)  │
            └───────┬───────┘
                    │
                    ▼
            ┌───────────────┐
            │    FILTER     │
            │ • nonStopOnly │
            │ • maxPrice    │
            │ • airlines    │
            │  (120 → 80)   │
            └───────┬───────┘
                    │
                    ▼
            ┌───────────────┐
            │     SORT      │
            │  • PRICE_ASC  │
            │  • DURATION   │
            │  • BEST       │
            └───────┬───────┘
                    │
                    ▼
            ┌───────────────┐
            │   PAGINATE    │
            │ Page 1 (50)   │
            │ Page 2 (30)   │
            └───────┬───────┘
                    │
                    ▼
            ┌───────────────┐
            │  50 OFFERS    │
            │   RETURNED    │
            └───────────────┘
```

---

**Architecture designed for scale! 🚀**
